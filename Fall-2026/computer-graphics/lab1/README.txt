Josiah Schmitz
Lab 1 - Computer Graphics

I have three shapes in this program in front of a gray canvas: a right triangle, an ellipse, and a regular polygon.

The triangle is blue and centered in the lower-left quadrant of the canvas, with the right angle on the upper-right of the triangle.
It has been drawn with LINE_STRIP, so it is not filled.

The ellipse is green and centered in the lower-right quadrant of the canvas, with a horizontal radius of 0.2 and a vertical radius of 0.1.
It has been with TRIANGLE_FAN, so it is filled.

The regular polygon currently is a yellow hexagon and is centered in the upper-right quadrant of the canvas.
It has been with TRIANGLE_FAN, so it is filled.
Additionally, it was drawn with a formula that generates a regular polygon with a given side length (var sideLength) and number of sides (var n).